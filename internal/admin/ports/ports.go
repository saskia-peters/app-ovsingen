// Package ports declares the port interfaces of the Admin hexagon (AD-1):
// the settings ports (SMTP, backup destinations, schedule catalog) that other
// modules consume as read-only consumers. Concrete interfaces land with Epic 3.
package ports